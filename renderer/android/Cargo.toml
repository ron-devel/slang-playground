[package]
name = "renderer-android"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
renderer-core = { path = "../core" }
ash = "0.38"
jni = "0.21"
ndk-sys = "0.6"
