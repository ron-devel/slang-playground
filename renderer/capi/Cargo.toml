[package]
name = "renderer-capi"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["rlib", "cdylib", "staticlib"]

[dependencies]
renderer-core = { path = "../core" }
ash = "0.38"

[build-dependencies]
cbindgen = "0.27"
