[package]
name = "bridge-adb"
version = "0.1.0"
edition.workspace = true

[dependencies]
tokio = { version = "1", features = ["net", "io-util"] }
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
